import { createPosition } from "../helpers/planetGridSchema";

export const INITIAL_POSITION = createPosition(0, 0, "N");
export const INITIAL_MAP_DIMENSION = { m: 10, n: 10 };
export const OBSTACLES_NUMBER = 20;
